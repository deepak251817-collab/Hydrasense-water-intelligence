pandas
numpy<2
# Installed & verified: 1.4.2 (pinned after Phase 6: the served model artifacts
# must load under one sklearn version; all Phase 5A-5E tests pass on 1.4.2,
# and the backend inference service shares this environment)
scikit-learn==1.4.2
matplotlib
joblib
# Phase 5D-2: XGBoost deterioration prediction
# Installed & verified: xgboost 3.2.0 (pip install xgboost)
xgboost>=2.0
# Phase 5E: SHAP explainability
# Installed & verified: shap 0.48.0 with numpy 1.26.4 (shap >= 0.49 requires numpy >= 2,
# which is incompatible with the pinned <2 stack used by pyarrow/matplotlib here)
shap>=0.48,<0.49
